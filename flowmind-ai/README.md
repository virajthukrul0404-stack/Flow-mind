# FlowMind

## Structure

- `frontend/`: the working Next.js app, including the landing site, login/signup with Google auth, dashboard, and API routes
- `backend/`: a placeholder folder for a future standalone backend service

## Current app entrypoint

Run the product from `frontend/`.
